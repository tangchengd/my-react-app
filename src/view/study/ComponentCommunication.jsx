import StudyPlaceholder from "./StudyPlaceholder";

export default function ComponentCommunication() {
  return (
    <StudyPlaceholder
      title="组件通信"
      description="这里可以后续补充 props、状态提升、context 和跨层级通信的学习示例。"
    />
  );
}
